import java.util.Scanner;

public class Main {

    static Scanner sc = new Scanner(System.in);

    public static void main(String[] args) {

        String result = "";

        // вибір функції
        String choice = sc.nextLine();

        switch (choice) {
            case "1":
                result = split();
                break;
            case "2":
                result = reversal();
                break;
            case "3":
                result = rtSplit();
                break;
            default:
                return;
        }

        System.out.println(result);
        sc.close();
    }

    // 1) Ввід рядка з мінімум 2 словами довжиною >= 3
    public static String split() {

        while (true) {
            String input = sc.nextLine();
            String[] words = input.trim().split(" ");

            int count = 0;
            for (String word : words) {
                if (word.length() >= 3)
                    count++;
            }

            if (count >= 2)
                return input;
        }
    }

    // 2) Перевернути весь рядок
    public static Stri
